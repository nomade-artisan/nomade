export interface Collection {
  id: number;
  name: string;
  slug: string;
  description: string | null;
  created_at: string;
}

export interface CollectionFormState {
  name: string;
  slug: string;
  description: string;
}
export interface Collection {
  id: number;
  name: string;
  slug: string;
  description: string | null;
  created_at: string;
}

export interface CollectionFormState {
  name: string;
  slug: string;
  description: string;
}
